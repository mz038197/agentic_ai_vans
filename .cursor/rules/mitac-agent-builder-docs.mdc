---
description: 神通／Mitac Agent Builder 官方文件固定目錄（Vault materials，不在本 repo）
alwaysApply: true
---

# 神通 Agent Builder 文件位置

官方手冊與操作說明一律在：

`G:\我的雲端硬碟\Obsidian\Agent\raw\materials\神通科技\Agent_Builder`

這是 Obsidian Agent vault 的 `raw/materials`，**不是**本 repo。讀文件時直接開這個絕對路徑，不要在 `agentic_ai_vans` 裡找 PDF／手冊。

## 何時用這條路徑

使用者說「神通文件」「神通手冊」「Mitac 說明書」「Agent Builder 操作說明」「MCP 操作說明」或要對照官方能力時，先列這個資料夾再讀檔。

目前可見檔名（會增減，以磁碟為準）：

- `MiTAC Agent Builder操作說明書V9_技能管理整合版_含目錄與截圖.pdf`
- `MiTAC MCP操作說明書_v1.1.4.pdf`

## 邊界

- 視為 `raw` 來源：**不修改、不刪、不搬**既有檔。摘錄、更正、課程改寫寫在本 repo 的課程 md，或寫進 `wiki/`。
- 本 repo 的 `handoff-mitac-agent-builder-api.md` 是 API 實作筆記，不是官方手冊；REST 細節可對照 handoff，產品操作以這個資料夾的 PDF 為準。
- 舊本機路徑 `C:\Users\mz038\Desktop\MitacAgentBuilder-API-docs-v1.2.2\` 不再當預設；除非使用者明確指定。
