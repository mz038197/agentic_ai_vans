# 課堂起點用安裝器寫入課程專案，通關成品維持單檔

通關成品（本 repo 根目錄 `main.py`）是示範用的完整單檔 Agent。課堂填空是另一組檔，由安裝器寫入學生的課程專案，不在 vans 裡跑。對齊既有的 Shell Agent 安裝器：來源在教材 repo，實例在學生那邊；課程專案已有同名檔則跳過。若改成讓學生在 vans 子目錄填、或把通關成品也拆成關卡檔，根目錄 `uv run main.py` 會跑錯檔，Shell 抽取角色設定也會對不準。
